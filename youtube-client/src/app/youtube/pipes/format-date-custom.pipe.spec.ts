import { FormatDateCustomPipe } from './format-date-custom.pipe';

describe('FormatDateCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
