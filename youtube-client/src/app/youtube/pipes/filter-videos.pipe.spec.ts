import { FilterVideosPipe } from './filter-videos.pipe';

describe('FilterVideosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVideosPipe();
    expect(pipe).toBeTruthy();
  });
});
