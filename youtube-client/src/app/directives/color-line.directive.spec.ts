import { ColorLineDirective } from './color-line.directive';

describe('ColorLineDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorLineDirective();
    expect(directive).toBeTruthy();
  });
});
