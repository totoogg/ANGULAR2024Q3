import { SortVideosPipe } from './sort-videos.pipe';

describe('SortVideosPipe', () => {
  it('create an instance', () => {
    const pipe = new SortVideosPipe();
    expect(pipe).toBeTruthy();
  });
});
